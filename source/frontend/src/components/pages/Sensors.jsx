import React from "react";

const Sensors = () => {
    return (
        <div className="page sensors-page">
            <h2>Sensors</h2>
            {/* Filtri Area + Measurement Type */}
        </div>
    );
};

export default Sensors;